import { StoryblokDirective } from './storyblok.directive';

describe('StoryblokDirective', () => {
  it('should create an instance', () => {
    const directive = new StoryblokDirective();
    expect(directive).toBeTruthy();
  });
});
